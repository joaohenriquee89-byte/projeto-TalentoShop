import React from 'react';
import Messages from '../lojista/Messages';

// Reuse the existing Messages component for now as the logic is identical
const VendedorMessages: React.FC = () => {
    return <Messages />;
};

export default VendedorMessages;
